# Task7 多图病例视图初判

这一步先看了当前新数据里 `17` 个原始多图病例，目标不是立刻给训练下最终结论，而是先判断“当前统一取图2”在视图类型上是否合理。

## 当前结论

- `17` 个多图病例里，当前被选中的 `图2` 明显偏 `cut_surface` 或 `mixed` 的占多数。
- 第一轮看完后，整体结果是：
  - `prefer_image2 = 15`
  - `prefer_image1 = 1`
  - `recheck_pair = 1`
- `batch1` 的 `11` 例里，初判：
  - `prefer_image2`: `9`
  - `recheck_pair`: `1`
  - `prefer_image1`: `1`
- `batch2` 的 `6` 例里，初判：
  - `prefer_image2`: `6`
- 也就是说，当前“多图统一取图2”不是完全没有道理，但并不总是对。

## 最值得注意的例子

- `2407584`
  - 当前选中的是 `图2`
  - 但从我们这轮看图判断，`图2` 更偏外观面，`图1` 的切面信息更强
  - 这是目前最明确的一个“图2 不一定优于图1”的例子

- `2202262`
  - 当前选中 `图2`
  - `图2` 是更典型的切面图，优于 `图1` 的整体外观

- `2202752`
  - 当前选中 `图2`
  - `图2` 也是更清楚的切面图，和医生之前的直觉一致

- `2214165`
  - 当前选中 `图2`
  - `图1` 还有手套和外观姿态干扰，`图2` 作为主图更合理

## 对 batch2 的当前判断

这轮也已经把 `batch2` 的 `6` 个多图病例按 `图1/图2` 成对看完。初判结果是：

- 这 6 例里，当前 `图2` 都比 `图1` 更接近可判读的切面或混合切面；
- 最典型的有：
  - `2206029`
  - `2118344`
  - `2212556`
- `2206144`、`2207835` 这种虽然不是非常标准的纯切面，但图2仍然比图1更有内部结构信息。

## 下一步

下一步最值得做的是：

1. 对 `17` 个多图病例都给出最终的：
   - `prefer_image1`
   - `prefer_image2`
   - `both_ok`
2. 再看 `Task7` 的高危漏诊病例里，是否明显集中在：
   - `outer_surface`
   - `mixed`
   - 非标准切面图

现阶段可以先记住一句话：

> 单图病例不能默认都是切面图；多图病例里“统一取图2”总体有一定合理性，但并不总是最优，至少已有明确反例。
